apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-for-dev-chart.fullname" . }}
  labels:
    {{- include "helm-for-dev-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-for-dev-chart.selectorLabels" . | nindent 4 }}
